(function(exports) {
  "use strict";

  function NotesList() {
    this.list = [];
  }

  List.prototype.add = function(text) {
    note = text;
    this.list.push(note);
  };

  exports.NotesList = NoteList;
})(this);
